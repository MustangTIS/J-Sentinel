# J-Sentinel 高度防災システム - インジェスト仕様書 (Draft v2.0)

## 1. 気象庁防災情報（地震・津波・気象・火山）のシステム仕様

### ① 取得方式（インジェスト）

* **方式**: **PULL型（定期ポーリング）**
* **データソース**: 気象庁が公開している公式のインデックス・APIエンドポイント（info系、quake系、keiho系）を定期ポーリング。
* **更新頻度・タイミング**:
* 一定間隔で各エンドポイントにリクエストを送り、新着データをチェック・同期。



### ② データの流れ（パイプライン）

1. **ポーリング**: 各種インデックス（一覧）へアクセスし、新着のエントリ（IDやタイムスタンプ）をチェック。
2. **重複排除（冪等性の担保）**: 既に取得・保存済みのデータやタイムスタンプであればスキップ。
3. **実体データの取得**: 新着があれば、個別のJSON/XMLファイルをダウンロード。
4. **ルーティング・保存**: アプリケーション直下のローカルストレージ内へ、独立したAPIソース別・ジャンル別に振り分けて保存。
* **ルーティング方針**: `etc`（未分類）行きになるデータを将来的には極力減らし、パース条件やキーワード判定の精度を継続的に高めて各正規カテゴリへ適切に吸収させる。



---

## 2. ディレクトリ構造と保存先ルーティング

```text
../j-sent/database/
├─info                       # info系API（各種速報・予報・火山等）
│   ├─alert                  # アラート情報
│   ├─etc                    # 未分類の行き先（※将来的には削減・正規化を推進）
│   ├─volcano                # 火山情報（info系から集約）
│   ├─warning                # 記録的短時間大雨情報や土砂災害警戒情報等
│   └─yoho                   # 長期予報等
├─keiho                      # 警報マップ・リアルタイム変換系
│   ├─base                   # 生データ（map.json等）
│   └─convert                # 名称変換済みの整形データ（WarningRT.json等）
└─quake                      # 地震系API（確定報・津波等）
    ├─etc                    # 未分類の行き先（※将来的には削減・正規化を推進）
    ├─japan                  # 日本国内の震度・震源確定情報
    ├─tsunami                # 津波関連情報
    └─world                  # 遠地地震情報

```

---

## 3. 配信（アウトプット）方式

* **方針**: **独立したプロセス・パイプラインによる運用**
* **配信先・挙動**:
* 取得した各種気象・地震・警報データは、処理の詰まりやエラーが他に影響を与えないよう、独立したパイプライン経由でDiscord等の通知先へ構造化して送信。
* 雑多な情報やログ用チャンネルへ淡々と流すことで、要塞環境としての安定稼働を維持する。